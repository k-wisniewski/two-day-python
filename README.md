# 2 dniowe, skrócone szkolenie z Pythona

## Setup repozytorium

### Środowisko wirtualne
W katalogu repozytorium wykonac:
`python -m venv env`
`. env/bin/activate`

### Instalacja zaleznosci:
`python -m pip install -r requirements.txt`